%h1 Dashboard

.row
  .col-sm-6
    %h3 Recent Games
    .games
      = render partial: "games/game", collection: @recent_games
  .col-sm-6
    %h3 Games to Confirm
    %p Please confirm or deny whether these games happened.
    - if @games_to_confirm.any?
      .games
        = render partial: "games/game", collection: @games_to_confirm
    - else
      .well
        You're all done!
    %h3 Pending Games
    %p These are games that the losers have not yet confirmed.

    - if @pending_games.any? 
      .games
        = render partial: "games/game", collection: @pending_games
    - else
      .well
        No pending games!
