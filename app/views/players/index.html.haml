%h1 Rankings

%table.table.table-striped
  %tr
    %th Rank
    %th Username
    %th Points
    %th # Games
  - @players.each do |player|
    %tr
      %td= player.ranking
      %td
        %strong= link_to player.username, player
      %td= number_with_precision(player.points, precision: 2)
      %td= player.games_count
